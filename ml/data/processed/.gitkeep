# Augmented images are NOT committed (regenerated by weather_augment.py).
# The manifest JSON files ARE committed (small, text-based).
# Run: python ml/scripts/weather_augment.py  to regenerate images.
