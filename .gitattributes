# Git LFS tracking for binary model weights
# Install Git LFS first: https://git-lfs.github.com/
# Then run: git lfs install

# Our trained model checkpoints
ml/models/**/*.pt filter=lfs diff=lfs merge=lfs -text
ml/models/**/*.pth filter=lfs diff=lfs merge=lfs -text
