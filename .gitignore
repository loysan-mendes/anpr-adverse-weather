# =============================================================================
# Python
# =============================================================================
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.env
.venv
env/
venv/
ENV/

# =============================================================================
# Virtual environment (ml/venv is gigabytes -- never commit this)
# =============================================================================
ml/venv/

# =============================================================================
# Large model weights -- tracked via Git LFS (see .gitattributes)
# If you don't have LFS, these are excluded entirely and downloaded separately.
# =============================================================================
# Trained checkpoints (our own trained models)
ml/models/quality_analyzer/best_model.pt
ml/models/restoration/blur/best_model.pt
ml/models/restoration/haze/best_model.pt
ml/models/restoration/rain/best_model.pt
ml/models/yolo_runs/plate_detector/weights/best.pt
ml/models/yolo_runs/plate_detector/weights/last.pt
ml/models/yolo_runs/plate_detector2/

# Pretrained base weights (downloaded automatically on first run)
*.pt
*.pth
*.onnx

# =============================================================================
# Raw dataset (large, downloaded by download_dataset.py -- not committed)
# =============================================================================
ml/data/raw/

# =============================================================================
# Augmented image data (regenerated by weather_augment.py -- not committed)
# Keep the manifest JSON (small, text-based) but not the image files.
# =============================================================================
ml/data/processed/augmented/
ml/data/processed/clear/

# =============================================================================
# YOLO dataset splits (regenerated by prepare_yolo_dataset.py)
# =============================================================================
ml/data/yolo_dataset/images/
ml/data/yolo_dataset/labels/

# =============================================================================
# Training artifacts / outputs (large, regeneratable)
# =============================================================================
ml/models/yolo_runs/*/
# Keep model dir structure but not run outputs
!ml/models/yolo_runs/.gitkeep

# =============================================================================
# PaddleOCR & PaddlePaddle cache (downloaded at runtime)
# =============================================================================
~/.paddleocr/
~/.paddlex/

# =============================================================================
# Jupyter / IPython
# =============================================================================
.ipynb_checkpoints/
*.ipynb

# =============================================================================
# IDEs & editors
# =============================================================================
.vscode/settings.json
.idea/
*.swp
*.swo
*~

# =============================================================================
# OS artifacts
# =============================================================================
.DS_Store
Thumbs.db
desktop.ini

# =============================================================================
# Logs & temp files
# =============================================================================
*.log
*.tmp
*.bak
*.zip

# =============================================================================
# EDA / preview images (regeneratable, binary -- not tracked)
# =============================================================================
ml/data/processed/augmentation_preview.png
ml/data/processed/eda_report/
ml/ml/

# =============================================================================
# YOLO run metrics/plots (regeneratable)
# =============================================================================
runs/
